# Linux-targeted files must stay LF even when checked out on Windows,
# otherwise a CRLF shebang/script breaks on the server.
*.sh        text eol=lf
Dockerfile  text eol=lf
*.conf      text eol=lf
# Windows-only scripts keep CRLF.
*.ps1       text eol=crlf
